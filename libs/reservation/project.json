{
  "name": "reservation",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "reservation/src",
  "prefix": "reservation",
  "projectType": "library",
  "tags": ["scope:reservation", "type:feature"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "storybook": {
      "executor": "@storybook/angular:start-storybook",
      "options": {
        "port": 4400,
        "configDir": "libs/reservation/.storybook",
        "browserTarget": "reservation:build-storybook",
        "compodoc": false
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "build-storybook": {
      "executor": "@storybook/angular:build-storybook",
      "outputs": ["{options.outputDir}"],
      "options": {
        "outputDir": "dist/storybook/reservation",
        "configDir": "libs/reservation/.storybook",
        "browserTarget": "reservation:build-storybook",
        "compodoc": false
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "test-storybook": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test-storybook -c libs/reservation/.storybook --url=http://localhost:4400"
      }
    },
    "static-storybook": {
      "executor": "@nx/web:file-server",
      "dependsOn": ["build-storybook"],
      "options": {
        "buildTarget": "reservation:build-storybook",
        "staticFilePath": "dist/storybook/reservation",
        "spa": true
      },
      "configurations": {
        "ci": {
          "buildTarget": "reservation:build-storybook:ci"
        }
      }
    }
  }
}
